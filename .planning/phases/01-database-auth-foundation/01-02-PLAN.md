---
phase: 01-database-auth-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Soundflare/src/lib/agency-auth.ts
  - Soundflare/src/app/agency/layout.tsx
  - Soundflare/src/app/api/agency/metrics/route.ts
  - Soundflare/src/app/api/agency/failures/route.ts
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "Unauthenticated users hitting /agency are redirected to /sign-in?next=/agency"
    - "Non-admin authenticated users hitting /agency are silently redirected to /"
    - "Admin users can access /agency and see children rendered"
    - "GET /api/agency/metrics returns aggregated metrics from agency_metrics RPC for admin users"
    - "GET /api/agency/failures returns failed call records from agency_failures RPC for admin users"
    - "Non-admin users calling /api/agency/* receive 401 or 403"
  artifacts:
    - path: "Soundflare/src/lib/agency-auth.ts"
      provides: "assertAgencyAdmin() shared helper and UnauthorizedError class"
      exports: ["assertAgencyAdmin", "UnauthorizedError"]
    - path: "Soundflare/src/app/agency/layout.tsx"
      provides: "Server component layout that checks auth + PYPE_ADMINS before rendering children"
      min_lines: 15
    - path: "Soundflare/src/app/api/agency/metrics/route.ts"
      provides: "GET handler calling agency_metrics RPC via service-role client"
      exports: ["GET"]
    - path: "Soundflare/src/app/api/agency/failures/route.ts"
      provides: "GET handler calling agency_failures RPC via service-role client"
      exports: ["GET"]
  key_links:
    - from: "Soundflare/src/app/agency/layout.tsx"
      to: "process.env.PYPE_ADMINS"
      via: "env var parsed as comma-separated emails"
      pattern: "PYPE_ADMINS.*split"
    - from: "Soundflare/src/app/api/agency/metrics/route.ts"
      to: "agency_metrics RPC"
      via: "getSupabaseAdmin().rpc('agency_metrics')"
      pattern: "rpc.*agency_metrics"
    - from: "Soundflare/src/app/api/agency/failures/route.ts"
      to: "agency_failures RPC"
      via: "getSupabaseAdmin().rpc('agency_failures')"
      pattern: "rpc.*agency_failures"
    - from: "Soundflare/src/app/api/agency/metrics/route.ts"
      to: "Soundflare/src/lib/agency-auth.ts"
      via: "import assertAgencyAdmin"
      pattern: "assertAgencyAdmin"
---

<objective>
Create the admin auth guard pattern and server-side API routes for agency metrics and failures. The layout protects the /agency route group. The shared helper protects API routes. Both API routes call the RPC functions created in Plan 01 via the service-role client.

Purpose: INFR-01 (admin-only access) and INFR-02 (server-side RPC calls, not client-side aggregation) are satisfied. Phase 2 will build the dashboard UI consuming these API routes.
Output: Auth guard layout, shared helper, two API route handlers.
</objective>

<execution_context>
@/home/sam/.claude/get-shit-done/workflows/execute-plan.md
@/home/sam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-auth-foundation/01-CONTEXT.md
@.planning/phases/01-database-auth-foundation/01-RESEARCH.md
@.planning/phases/01-database-auth-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Existing auth and Supabase patterns from codebase -->

From Soundflare/src/utils/supabase/server.ts:
```typescript
// Creates a cookie-based Supabase client for server components/route handlers
export async function createClient(): Promise<SupabaseClient>
```

From Soundflare/src/lib/supabase-server.ts:
```typescript
// Returns a service-role Supabase client (bypasses RLS)
export function getSupabaseAdmin(): SupabaseClient
```

From Soundflare/src/lib/auth.ts:
```typescript
// Wraps supabase.auth.getUser() -- returns user or null
export async function currentUser(): Promise<User | null>
```

From Soundflare/src/app/api/email/notify-admins/route.ts:
```typescript
// Existing PYPE_ADMINS parsing pattern:
const adminEmails = (process.env.PYPE_ADMINS || "").split(",").map(e => e.trim().toLowerCase())
```

Supabase RPC call pattern:
```typescript
const { data, error } = await getSupabaseAdmin().rpc('function_name', { param1: value1 })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth guard helper and agency layout</name>
  <files>Soundflare/src/lib/agency-auth.ts, Soundflare/src/app/agency/layout.tsx</files>
  <action>
1. Create `src/lib/agency-auth.ts` with:
   - `UnauthorizedError` class extending Error with a `status` property (default 401)
   - `assertAgencyAdmin()` async function that:
     a. Creates Supabase client via `createClient()` from `@/utils/supabase/server`
     b. Calls `supabase.auth.getUser()` -- throws UnauthorizedError(401) if no user
     c. Parses `process.env.PYPE_ADMINS` as comma-separated lowercase emails
     d. Throws UnauthorizedError(403) if user.email not in admin list
     e. Returns the user object on success
   - Follow the exact pattern from the research document Pattern 2

2. Create `src/app/agency/layout.tsx` as a server component:
   - Import `redirect` from `next/navigation`
   - Import `createClient` from `@/utils/supabase/server`
   - Get user via `supabase.auth.getUser()`
   - If no user: `redirect('/sign-in?next=/agency')`
   - Parse PYPE_ADMINS, compare lowercased emails
   - If not admin: `redirect('/')`
   - Return `<>{children}</>`
   - Follow the exact pattern from research document Pattern 1

3. Ensure PYPE_ADMINS is in `.env.docker` (check first, add if missing):
   - Value: `admin@soundflare.ai` (the default login user per CLAUDE.md)
   - Also add to `.env` / `.env.local` if those exist for local dev
  </action>
  <verify>
    <automated>cd /root/projects/Soundflare2/Soundflare && npx tsc --noEmit --pretty 2>&1 | grep -E "(agency-auth|agency/layout)" || echo "No type errors in agency files"</automated>
  </verify>
  <done>agency-auth.ts exports assertAgencyAdmin and UnauthorizedError. Agency layout.tsx redirects unauthenticated to /sign-in?next=/agency, non-admins to /, renders children for admins. PYPE_ADMINS env var is set.</done>
</task>

<task type="auto">
  <name>Task 2: Create agency API routes for metrics and failures</name>
  <files>Soundflare/src/app/api/agency/metrics/route.ts, Soundflare/src/app/api/agency/failures/route.ts</files>
  <action>
1. Create `src/app/api/agency/metrics/route.ts`:
   - Export async function GET(request: NextRequest)
   - Call `assertAgencyAdmin()` -- catch UnauthorizedError, return JSON error with correct status
   - Extract `start_date` and `end_date` from URL searchParams (optional, RPC has defaults)
   - Call `getSupabaseAdmin().rpc('agency_metrics', { start_date, end_date })` -- only pass params if provided
   - Return `NextResponse.json(data)` on success
   - Return `NextResponse.json({ error, details }, { status: 500 })` on RPC error
   - Follow existing API route error handling pattern from CONVENTIONS.md

2. Create `src/app/api/agency/failures/route.ts`:
   - Export async function GET(request: NextRequest)
   - Call `assertAgencyAdmin()` -- same auth pattern
   - Extract `start_date`, `end_date`, `error_type` from searchParams (all optional)
   - Call `getSupabaseAdmin().rpc('agency_failures', { start_date, end_date, error_type })` -- only pass non-null params
   - Return `NextResponse.json(data)` on success
   - Same error pattern

Both routes:
- Use `getSupabaseAdmin()` from `@/lib/supabase-server` (service-role client, bypasses RLS)
- Do NOT use the browser Supabase client
- Follow existing error response format: `{ error: string, details?: string }`
- Include console.error for error logging
  </action>
  <verify>
    <automated>cd /root/projects/Soundflare2/Soundflare && npx tsc --noEmit --pretty 2>&1 | grep -E "(api/agency)" || echo "No type errors in agency API routes"</automated>
  </verify>
  <done>GET /api/agency/metrics returns metrics data for admin users, 401/403 for non-admins. GET /api/agency/failures returns failure records with optional error_type filter. Both routes use service-role client exclusively.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd Soundflare && npx tsc --noEmit`
2. Build succeeds: `cd Soundflare && npm run build` (confirms no SSR issues)
3. API routes respond correctly for admin user (manual curl after build, or via the running app)
4. Auth guard redirects work (verify layout.tsx renders without runtime errors)
</verification>

<success_criteria>
- assertAgencyAdmin() reusable across all /api/agency/* routes
- /agency layout redirects unauthenticated to /sign-in?next=/agency, non-admins to /
- /api/agency/metrics returns RPC data via service-role client
- /api/agency/failures returns RPC data with optional error_type filter via service-role client
- No browser-client Supabase usage in agency code
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-auth-foundation/01-02-SUMMARY.md`
</output>
